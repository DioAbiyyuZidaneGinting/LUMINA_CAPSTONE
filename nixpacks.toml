[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install -r API/requirements.txt"]

[start]
cmd = "cd API && python -m uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
